draw_polygon = function() {
    var points = new Array();

    var add_point = function(point) {
        points[point.length] = point;
    }
}

function noise() {
    return (Math.random() - 0.5) * 3;
}
